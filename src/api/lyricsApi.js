// Simulated Lyrics API

// This is a placeholder function. You can replace it later with real API logic.
// Currently returns sample Sinhala lyrics.

export const getLyricsFromYoutube = async (youtubeLink) => {
  console.log("Fetching lyrics for:", youtubeLink);

  // Simulate processing delay
  await new Promise((resolve) => setTimeout(resolve, 1000));

  // Sample Sinhala lyrics (replace with actual lyrics logic or API call)
  return `සඳ එළිය වගේ පියඹන්න
මගේ මතකේ හිතට තියනවා
ඔබේ හඬට තවදුරටත් මට සවන් දිය නොහැක
සඳවතෙකු සේ හැමදාම ඔබ මතක් වෙලා තියනවා`;
};
